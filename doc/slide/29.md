### 29: Virtual FORTH Machine

```py
       ## FVM memory:
D = [] ## Data stack
R = [] ## Return stack
W = {} ## vocabulary Words
M = [] ## main Memory
```
```py
## `( -- )` dump FVM state
def dump(): print(f'\nD:{D}\nR:{R}\nW:{W}\nM:{M}')
```
```
D:[]    R:[]    W:{}    M:[]
```

## subtitles
### ru

Определяем три главных структуры форт-ядр`А: два стека и словарь.

Для вывода отладочной информации определяем первую команду вирту`альной машины:
`dump`. Она будет выводить эти структуры пользуясь средствами Python, и в его же
текстовом представлении.
