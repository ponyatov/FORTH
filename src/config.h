#ifndef _H_CONFIG
#define _H_CONFIG
                                // main memory size, bytes
#define Msz 0x1000
                                // return stack size, cells
#define Rsz 0x100
                                // data stack size, cells
#define Dsz 0x10
                                // limit max number of VM.ticks
#define TICK_LIMIT (tick<0x10)

#endif // _H_CONFIG
